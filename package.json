{
  "name": "koyuje-website",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "npx serve .",
    "build": "node scripts/generate-runtime-config.js",
    "check": "node scripts/generate-runtime-config.js && node scripts/check-static-site.js"
  }
}
